# 版本紀錄

版本號用 [語意化版本](https://semver.org/lang/zh-TW/)：`主版本.次版本.修訂號`。

這個專案的判斷標準：

| 位置 | 什麼時候加 | 例子 |
|---|---|---|
| **主版本** | 使用者存的資料要遷移，或既有網址／API 的行為改變 | 課程 JSON 結構改掉、登入方式換掉 |
| **次版本** | 加了新功能 | 多一個語言、多一段課程內容、新的 API 端點 |
| **修訂號** | 修 bug、改文案、調樣式 | 正規式寫錯、排版沒對齊、錯誤訊息不清楚 |

**進版一律要經過使用者同意。** 流程是 `./tools/release.sh <版號>`，它會更新
`package.json` 與 `public/shared/ll.js` 的版本字串、寫進這份 CHANGELOG、打 git tag。

---

## 未發布

### 新增
- **課程制**。原本只有一個軸（學習語言），說明語言寫死繁體中文。
  加入泰語母語者的課程之後變成兩個軸，網址改成 `/{母語}-{學習語言}/`：
  `zh-en`、`zh-ja`、`zh-th`、`th-zh`、`th-en`
- **入口選擇頁**。兩步：先選母語、再選想學什麼。**選了泰文整頁就變泰文**，
  泰國使用者從第一個畫面開始就不用看中文。選擇記在 localStorage
- **泰語母語者學中文 `/th-zh/`**（簡體字 + 漢語拼音）
  - 中文跟泰文一樣不用空格斷詞，所以直接沿用泰文頁那套「目標語言在上、
    讀音在下」的堆疊排版，排版邏輯一行都沒重寫
  - prompt 全部用泰文寫。traps 特別針對**泰語聲調干擾** ——
    泰語五個調、中文四個調，泰國學習者會把泰語調值套到中文上
- **泰語母語者學英文 `/th-en/`**
  - 渲染跟 `zh-en` 相同（英文有空格、用 IPA）
  - **但 traps 是重寫的不是翻譯的**：泰語母語者的坑是尾音簡化
    （rice → rai、cats → cat）、子音串插入母音（street → sa-treet）、
    沒有動詞變化／複數／冠詞。照抄華語版會變成在教錯的東西

### 變更
- **不做舊網址轉址**。`/en/` `/ja/` `/th/` 直接 404 —— 課程化之前用的人還很少，
  留轉址反而讓路由多一層要維護的東西
- API 從 `{ lang }` 改成 `{ course }`。仍然吃舊格式，因為使用者瀏覽器裡
  可能還跑著 service worker 快取的舊版 JS
- service worker 版號跳到 v2，把課程化之前的快取清掉

### 修正
- `plain()` 的標記剝除正規式被早先的全域改名弄壞成 `<(en|word|thai)>`，
  導致 `<zh>` 標記剝不掉。改成 `<(en|jp|thai|zh)>`

---

## 1.1.0 — 2026-09-13

### 新增
- **泰文版 `/th/`**。骨架從日文版長出來（兩者都不用空白斷詞、都靠模型給的
  token 陣列切詞），但讀音的呈現完全不同：
  - **不用 ruby**。泰文的母音符號在上、聲調符號更在上、另一組母音在下，
    上下都被佔滿，轉寫疊上去會糊成一團。改成**例句逐詞上下堆疊**
    （泰文在上、Paiboon 在下），內文則用括號跟在詞後面
  - 轉寫用 **Paiboon**（有標聲調），與泰文字卡庫一致。實測模型產出的
    `pǒm`／`bpen`／`kráp`／`kâ` 與字卡庫 3,132 張卡的拼法**完全一致**
  - 聲調欄用中平／低／降／高／上升，沿用字卡專案 `tone.ts` 的命名
  - 字型 Sarabun（有頭圈），行高 2.2
  - 泰文的 Standard 只有一個音色，所以拿掉男女語音切換

### 修正
- `hydrate` 會用伺服器的清單**整個覆寫**本機，把首次開啟的示範課程洗掉。
  改成合併：伺服器的排前面，本機獨有的接在後面

---

## 1.0.0 — 2026-09-13

第一個上線版本。https://wordmatch.lucas860529.workers.dev

### 新增
- **英文 `/en/` 與日文 `/ja/`**。這兩個 app 是先前在 Claude artifact 環境做好的成品，
  這一版做的是「可部署化」——UI、六段渲染、ruby、單字總表全部原樣保留，
  只把 `window.claude` 換成 `/api/lesson`
- **登入系統**。整站封閉，密碼放 Cloudflare Secret `ACCESS_CODES`，
  一行一組「名字:密碼」。沒有註冊流程也沒有使用者資料表
- **D1 持久化歷史**。課程存伺服器，localStorage 降為本機快取，換裝置也還在
- **`/api/lesson`**（Gemini 代理）與 **`/api/tts`**（Google TTS 代理）。
  prompt 只存在伺服器端 —— 否則任何人都能把金鑰當免費的通用 LLM 用
- **TTS 成本護欄五層**，其中全域每日字元上限與 IP 無關，換 IP 灌也沒用
- **PWA**：manifest、service worker（`/api/*` 不進快取）、手寫 PNG 產生的圖示

### 與原始架構文件的差異
- **部署用 Workers 不用 Pages**（ADR 0004）。Cloudflare 官方已明講新專案要用
  Workers，後台的 Create 流程也不再提供 Pages 需要的欄位
- **`gemini-2.5-flash` 對新帳號已停止供應**，改用 `gemini-3.6-flash`
- **TTS 護欄按字元算不按次數**（ADR 0003）。Google 按字元收費，
  「500 次」可能是 7,500 字元也可能是 100,000 字元
- **沒有串流**。artifact 版會顯示「現在寫到：例句」，現在只顯示經過秒數
